" 配色方案
if has('gui_running')
    "set background=dark
    set background=light
 
    colorscheme solarized
    "colorscheme molokai
    "colorscheme phd
else
    set background=dark
    "set background=light
    colorscheme solarized
    "colorscheme phd
endif
