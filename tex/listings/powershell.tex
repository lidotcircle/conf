% Define {listings:Language : MYPSH}
\ifdefined\texrc@package@config@listings@powershell\else
\def\texrc@package@config@listings@powershell{LOADED}

%{{{ New listings language: MYBASH => for bash

%% Define Highlight Colors
\lstdefinelanguage{MYPSH}{%
    sensitive=true,
    basicstyle=\ttfamily,
    %{{{ keywords
    keywordsprefix=\$,%
    keywords={\$null, \$false, \$true},%
    morekeywords=[2]{eq, ne, ge, gt, lt, le, like, notlike, match, 
        notmatch, replace, split, contains, notcontainsieq, ine, ige, igt, 
        ile, ilt, ilike, inotlike, imatch, inotmatch, ireplace, isplit, icontains, 
        inotcontains, ceq, cne, cge, cgt, clt, cle, clike, cnotlike, 
        cmatch, cnotmatch, creplace, csplit, ccontains, cnotcontains, in, notin, is, 
        isnot, as, join, and, or, not, xor, band, bor, bnot, bxor
    },%
    morekeywords=[3]{begin, process, end, exit, inlinescript,
        parallel, sequence
    },%
    morekeywords=[4]{try, catch, finally, throw, return,
        filter, in, trap, param, data,
        dynamicparam, return, filter, in, trap, param, data, dynamicparam
    },%
    morekeywords=[5]{if, else, elseif, switch, default,
        while, for, do, until, break, continue, foreach, in
    },%
    keywordstyle={\bfseries\color{prog@vari}},%
    keywordstyle=[2]{\bfseries\color{prog@oper}},%
    keywordstyle=[3]{\bfseries\color{prog@keyw}},%
    keywordstyle=[4]{\bfseries\color{prog@keyw}},%
    keywordstyle=[5]{\bfseries\color{prog@cond}},%
    %}}}
    %{{{ comments
        comment=[l]{\#}, % line comments
        commentstyle=\it\small\color{prog@comm},%
    %}}}
    %{{{ String
        morestring=[b]",%
        morestring=[b]',%
        stringstyle=\color{prog@stri}%
    %}}} End String
}
%}}}

\fi
